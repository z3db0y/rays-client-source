{
  "name": "rays-client",
  "version": "1.0.7",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "npx electron .",
    "build:win": "npx electron-builder --win",
    "build:linux": "wsl npx electron-builder --linux",
    "build:mac": "npx electron-builder --mac",
    "build": "npx electron-builder",
    "dist": "npx electron-builder --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.4.7",
    "electron-builder": "^22.11.3"
  },
  "dependencies": {
    "bytenode": "^1.3.6",
    "discord-rpc": "^4.0.1",
    "electron-store": "^8.0.1",
    "electron-updater": "^5.0.1",
    "tmi.js": "^1.8.5"
  }
}
